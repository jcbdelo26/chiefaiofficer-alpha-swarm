# ============================================
# Chief AI Officer Alpha Swarm - Production Dependencies
# ============================================

# Core
python-dotenv>=1.0.0
requests>=2.31.0
aiohttp>=3.9.0
pydantic>=2.5.0
fastapi>=0.109.0
uvicorn[standard]>=0.25.0

# Database
supabase>=2.0.0
redis>=5.0.0

# Agent Framework
inngest>=0.4.0
anthropic>=0.8.0
openai>=1.6.0

# Session / Auth (required by SessionMiddleware + login form)
itsdangerous>=2.1.0
python-multipart>=0.0.6

# Utilities
httpx>=0.26.0
python-dateutil>=2.8.0
pytz>=2023.3
tenacity>=8.2.0

# Note: Heavy dev dependencies (selenium, chromadb, whisper) 
# moved to requirements.dev.txt to ensure stable Railway builds.
